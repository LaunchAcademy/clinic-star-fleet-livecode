<h1>Ships</h1>
<ul>
  <% @ships.each do |ship| %>
    <li>
      <a href="/starships/<%= ship.id %>"> <%= ship.name %> </a>
    </li>
  <% end %>
</ul>

<a href="/starships/new">Create your Own Starship!</a>
